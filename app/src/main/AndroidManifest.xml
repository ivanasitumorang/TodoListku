<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="azuka.com.todolistku">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddTodoActivity"
            android:label="@string/title_activity_buat_todo"/>
        <activity android:name=".UpdateTodoActivity"
            android:label="@string/title_activity_update_todo"/>
        <activity android:name=".LihatDetailActivity"
            android:label="@string/title_activity_lihat_todo"></activity>
    </application>

</manifest>